#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>
#define N 10000000000

int main()
{
	int *ara;
	ara = (int *)malloc(sizeof(int) * N);
	int i = sqrt(N);
	for (int i)
		return 0;
}